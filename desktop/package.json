{
  "name": "loopwright-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tauri desktop shell for the Loopwright actor-critic engine",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:sidecar": "node ../scripts/build-sidecar.mjs",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.6.3",
    "vite": "^8.0.16"
  }
}
